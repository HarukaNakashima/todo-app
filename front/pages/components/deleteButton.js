import React from "react"
import axios from "axios"

//taskの削除
//TODO:削除ボタンを押すと、taskListからそのtaskが削除される
export default function DeleteButton() {
    return (
        <div>
        <button>削除</button>
    </div>
    );
}